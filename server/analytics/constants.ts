/**
 * Canonical device-class value for automated, non-human traffic (link-preview
 * crawlers, social unfurlers, scrapers). Stored on scan events for
 * transparency but excluded from human-facing scan counts and breakdowns.
 */
export const SCAN_BOT_DEVICE_CLASS = "bot";
